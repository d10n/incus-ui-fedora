# incus-ui-fedora

Turnkey script for packaging incus-ui for fedora.

Just run `./build-rpm.sh`, then `sudo dnf install` the RPM in `./OUTPUT/noarch/incus-ui-*.rpm`

Note: documentation needs to be separately installed for the internal documentation links to work.
